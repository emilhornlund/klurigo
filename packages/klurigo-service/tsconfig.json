{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    /* Targets */
    "target": "ES2023",

    /* Modules & Resolution */
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "resolvePackageJsonExports": true,

    /* Decorators (NestJS) */
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,

    /* Interop & ergonomics */
    "allowSyntheticDefaultImports": true,
    "isolatedModules": true,

    /* Emit */
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true,
    "sourceMap": true,
    "removeComments": true,

    /* Strictness & checks */
    // The service retains its existing relaxed checks while strict null checks
    // remain enabled for the NestJS application.
    "strict": false,
    "strictNullChecks": true,
    "strictPropertyInitialization": false,
    "noImplicitAny": false,
    "strictBindCallApply": false,
    "noFallthroughCasesInSwitch": false,

    /* Tooling / Build info */
    "incremental": true,
    "tsBuildInfoFile": "./dist/tsconfig.tsbuildinfo"
  },
  "exclude": [
    "dist",
    "node_modules",
    "test",
    "scripts",
    "test-utils",
    "src/**/*.spec.ts",
    "src/**/*.test.ts",
    "src/**/*.e2e-spec.ts"
  ]
}
